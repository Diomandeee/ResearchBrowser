# TestFlight Upload — Tag-triggered
# Auto-generated by openclaw-ios-bootstrap.sh

name: TestFlight

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      build_number:
        description: 'Build number override'
        required: false
        type: string

jobs:
  upload:
    uses: Diomandeee/.github/.github/workflows/reusable-testflight.yml@main
    with:
      scheme: 'ResearchBrowser'
      bundle-id: 'com.openclaw.researchbrowser'
      build-number-override: ${{ inputs.build_number || '' }}
      supabase-log: true
    secrets: inherit
